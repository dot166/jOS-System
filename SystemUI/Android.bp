android_app {

    name: "jOSSystemUI",
    use_resource_processor: true,

    libs: ["SystemUIPluginLib"],

    certificate: "platform",
    optimize: {
        enabled: false,
    },

    srcs: ["src/**/*.java"],

    static_libs: [
        "j.Lib",
    ],

    platform_apis: true,
}