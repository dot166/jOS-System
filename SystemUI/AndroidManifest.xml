<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jOS.System.UI">

    <permission android:name="com.android.systemui.permission.PLUGIN" />

    <application
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service android:name=".TestButtonProvider"
            android:exported="false"
            android:label="@string/plugin_label_test">
            <intent-filter>
                <action android:name="com.android.systemui.action.PLUGIN_LOCKSCREEN_RIGHT_BUTTON" />
                <action android:name="com.android.systemui.action.PLUGIN_LOCKSCREEN_LEFT_BUTTON" />
            </intent-filter>
        </service>
    </application>

</manifest>