android_app {
    name: "jOS-EasterEgg",
    platform_apis: true,
    certificate: "platform",

    resource_dirs: ["res"],

    srcs: [
        "java/**/*.kt",
        "java/**/*.java",
    ],

    manifest: "AndroidManifest.xml",

    static_libs: ["jOS.System.internal"],

    kotlincflags: ["-Xjvm-default=enable"],
}
